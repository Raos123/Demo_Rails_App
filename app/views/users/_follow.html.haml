= form_for(current_user.relationships.build(followed_id: @user.id), remote: true) do |relationship|
  %div
    = relationship.hidden_field :followed_id
  = relationship.submit "Follow", class: "btn btn-large btn-primary"
