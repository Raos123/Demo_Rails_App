= form_for(@micropost) do |micropost|
  = render 'shared/error_messages', object: @micropost
  .field
    = micropost.text_area :content, placeholder: "Compose new micropost..."
    %span#countdown 140 characters left.
  = micropost.submit "Post", class: "btn btn-large btn-primary"
